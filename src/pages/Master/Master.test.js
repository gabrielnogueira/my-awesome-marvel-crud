import Master from './Master';

it('fetch characters with search text', () => {
//   const comp = shallow(<Master />);
//   comp.find('AppBar').simulate('onChange', 'hulk');
//   expect(comp.props().searchText).toEqual('hulk');
});
